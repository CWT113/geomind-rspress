---
tag: new
---

import { Tabs, Tab } from '@rspress/theme-default';

# 基础入门

## 高亮块

:::note
这是一个 `note` 类型的 `block`
:::

:::tip
这是一个 `tip` 类型的 `block`
:::

:::info
这是一个 `info` 类型的 `block`
:::

:::warning
这是一个 `warning` 类型的 `block`
:::

:::danger
这是一个 `danger` 类型的 `block`
:::

:::details
这是一个 `details` of type `block`
:::

:::tip 自定义标题
自定义标题的 `block`
:::

:::tip{title=自定义标题}
自定义标题的 `block`
:::

## Github 方式
> [!NOTE]
> 这是一个 `note` 类型的 `block`

> [!TIP]
> 这是一个 `tip` 类型的 `block`

> [!INFO]
> 这是一个 `info` 类型的 `block`

> [!WARNING]
> 这是一个 `warning` 类型的 `block`

> [!DANGER]
> 这是一个 `danger` 类型的 `block`

> [!DETAILS]``` jsconst num = 10;
> ```

## 代码块
``` js
const num = 10;
```

### 代码块高亮

```js {2}
// hello, world
const number = 10;

function add(a, b) {
  return a + b;
}
```

### 代码块标题

```js title="demo.js" {2}
// hello, world
const number = 10;

function add(a, b) {
  return a + b;
}
```

### 代码块diff

```diff
function test() {
- console.log('deleted');
+ console.log('added');
  console.log('unchanged');
}
```

### 代码组

<Tabs>
  <Tab label="代码">
    ``` js
    const number = 10;

    function add(a, b) {
      return a + b;
    }
    ```
  </Tab>

  <Tab label="rspress.config.ts">
    ``` ts
    const number = 10;
    ```
  </Tab>
</Tabs>
